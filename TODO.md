# TODO — matchpoint engineering backlog

Ordered by priority. Human-only steps (API keys, dashboard config, store accounts) live in `MANUAL_TODOS.md`. Full context in `HANDOFF.md`.

## Next up
- [~] **T9 — lost-email recovery Phase B (recovery codes)** — **BUILT on `feat/t9-recovery-codes`** (2026-07-23), PR open. Decisions locked: 8 codes/set · auto-generate right after upgrade · lockout 5 fails/15 min per email · regenerate voids old set. Migration `011` (`recovery_codes` + `recovery_redeem_attempts` + `recovery_codes_remaining()` RPC + `user_id_for_email()` service_role helper); edge fns `issue-recovery-codes` (JWT ON) + `redeem-recovery-code` (`--no-verify-jwt`, admin `generateLink` mints session, no email); `lib/recovery-logic.ts` (100% cov); `AuthProvider` `issueRecoveryCodes`/`redeemRecoveryCode`/`codesRemaining`; `/account` recovery section + redeem flow. All static checks pass (jest 30, deno 18, typecheck, lint, hex-grep 0, expo web export). Backend deployed 2026-07-24 (migration 011 + both functions). **Live E2E PASSED** (browser, real backend): generate 8 codes → sign out to empty session → redeem email+one code → same account restored inbox-free, count went 8→7 (single-use consume), zero console errors. Review fix landed: `redeem-recovery-code` now returns HTTP 200 for expected failures so `functions.invoke` surfaces the message (was swallowed as generic non-2xx) — **needs a redeploy** (MANUAL_TODOS). Spec: `docs/superpowers/specs/2026-07-23-t9-recovery-codes-design.md`.
- [ ] **Account Settings hub + price-filter fix** — plan: `docs/superpowers/specs/2026-07-23-account-settings-and-price-filter-plan.md`. (1) **Price filter is broken** — `filterDeck` ([lib/deck.ts:17](lib/deck.ts)) always passes `price_level == null`, and Places (New) often omits price, so narrowing tiers hides nothing; filter state is also ephemeral (resets each deck visit). Diagnose null share in DB first, add an "Unpriced" toggle, persist the choice. (2) **Settings hub** — one sectioned Settings screen holding Account (email), Notifications (stub), Price range, and Locations (room-scoped); drop the top-level room-redirect and gate only Locations on `room`. Decisions needed before build: price-filter scope (per-user vs per-room) + persistence (AsyncStorage vs DB) — see plan.
- [ ] **T16 security fixes** (audit done 2026-07-22):
  - [x] `get-restaurants`: cap `location` length (80) + restrict to caller's `room.locations`. Merged (PR #2) and **deployed live** (`supabase functions deploy get-restaurants` run 2026-07-22).
  - [ ] Enable CAPTCHA + tighten rate limits on anonymous sign-in (Supabase Auth settings → MANUAL_TODOS). CAPTCHA also needs app-code (pass `captchaToken` on auth calls) before enabling, or it breaks live auth.
- [x] **T9 login (email)** — DONE: merged in **PR #9** (2026-07-23), verified live end-to-end (Resend SMTP + 6-digit template; upgrade, room-survival, sign-out re-anonymize, recovery sign-in all pass). Passwordless email OTP: `AuthProvider` + pure `lib/auth-logic.ts` (100% cov) + `/account` screen + settings entry + additive `SessionProvider` auth listener. Fixed OTP-length 8→6 (MANUAL_TODOS constraint).
- [ ] **T9 login (Google + Apple)** — deferred: needs OAuth dashboard config (Google Cloud client IDs, Apple Developer capability + Services ID) wired into Supabase Auth, then `linkIdentity` paths in `AuthProvider`. Apple mandatory on iOS once Google offered (App Store rule). Enforce no same-email auto-merge (no cross-provider override).

## Later
- [ ] **Harden OTP-length coupling (minor)** — the app hardcodes a 6-digit code (`lib/auth-logic.ts` `isValidCode` `/^\d{6}$/`, `app/account.tsx` `maxLength={6}`). If Supabase's Email OTP Length is ever set ≠6, codes silently fail to verify with no clear signal (hit live 2026-07-23, project was 8). Now locked by a MANUAL_TODOS constraint; optionally make the app tolerate a configurable length or surface a clearer "wrong code length" hint.
- [x] **T9 — session TTL policy (research + decide)** — DONE 2026-07-23. Decision: keep indefinite refresh for BOTH sessions, do not bound. Bounding buys no real risk reduction vs. this threat model, and bounding the anonymous session = permanent room loss (membership tied to anon UID). Real control (rotation + reuse detection) is already on by default. Doc: `docs/session-ttl-research.md`; dashboard verify → MANUAL_TODOS. Validated user's "bounded is just as secure" prior: technically true, practically pointless here.
- [~] **T9 — lost-email account recovery** — **Phase A DONE + merged** (PR #9, 2026-07-23): warning on `/account` upgrade view. **Phase B moved to "Next up"** (recovery codes) now that PR #9 is merged. Google/Apple (deferred) becomes the alternate factor too — needs carve-out from "no cross-provider override".
- [ ] **Offline vacation photos** — `mapSeedToItems` (`lib/session-logic.ts`) hardcodes `image_url: null`, so offline demo mode ignores the `image_url` T15 added to `data/seed.json`. Online (migration 010) is fine. If offline photos are wanted, read `row.image_url` in the map (needs `SeedRow.image_url?`).
- [ ] **T17 image caching** — make item photos (restaurant Google, vacation Wikimedia) fast + durable instead of hotlinked every render. Phased plan: (1) client cache — swap `SwipeCard` Image for `expo-image` (built-in memory+disk cache) + prefetch the next card; (2, optional/ToS-gated) persistent mirror to Supabase Storage for ToS-safe sources ONLY (Wikimedia yes + attribution; Google Places photo bytes NO — their ToS restricts storing them). Decide goal first: speed vs. decoupling from third parties.
- [ ] **T13 multiple rooms** — many rooms per user + a switcher. Schema change: `members.id = auth.uid()` (one room/user) → a membership table (many-to-many). Pairs with T9 for durable identity.
- [ ] **Adversarial QA subagent pass** — user requested; run once the account session limit resets. Break flows, fuzz inputs, retest RLS/rate limits.

## Testing (new — enforced from now on)
- [x] **Test suite + CI coverage gate** — jest-expo + RNTL (app logic) and `deno test` (edge function), 90% coverage gate scoped to `lib/`. GitHub Actions `test` + `deno-test` jobs gate deploy; husky `commit-msg` (commitlint) + `pre-push` (typecheck/lint/test) block bad commits; `.claude` PreToolUse hook reminds caveman-review before git. Branch protection = MANUAL_TODOS.

## Done (this session — all deployed)
Pairing/invite code · share link · swipe deck · realtime matches · date-night randomizer (T8) · locations + restaurants w/ Google photos + price filter (T7, T14) · per-item emoji (T12a) · Split Heart redesign + theme toggle · join-by-code defect fix (T10) · security audit + fix (T16, deployed) · invite link custom domain · swipe-card viewport fix · **travel photos for all 45 vacations (T15, migration 010 pushed live)** · test suite + CI coverage gate (PR #6, merged; branch protection on).
